#!/bin/bash
# cca — Claude Code with Ollama safety hook
# Usage: cca [any claude args...]
# Equivalent to: claude --settings hook-settings.json [args...]
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec claude --settings "$SCRIPT_DIR/hook-settings.json" "$@"
